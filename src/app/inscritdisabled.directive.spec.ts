import { InscritdisabledDirective } from './inscritdisabled.directive';

describe('InscritdisabledDirective', () => {
  it('should create an instance', () => {
    const directive = new InscritdisabledDirective();
    expect(directive).toBeTruthy();
  });
});
